// Conversion from string to array

let inputStr = "My name is Sid";

let arr = inputStr.split(" "); // [ 'My', 'name', 'is', 'Sid' ]
//replaces the input (" ") with a comma (,) to separate different values of an array.

console.log(arr);