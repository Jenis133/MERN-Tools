let sum = function(num1,num2) {
    let num3 = num1+num2;
} // end of function => returns by default

let s = sum (1,2);
console.log(s);

// If the value is not returned then by default the function will return an undefined value