let info = {
    name:"nitan", 
    age:29, 
    isMarried:false, 
    name:"ram"
};
console.log(info);

// In object duplicate key does not exist
// If it exists then the final value will overwrite the previous value
// In above code Nitan is replaced by Ram