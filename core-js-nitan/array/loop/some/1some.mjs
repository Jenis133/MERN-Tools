/* 
syntax
execution
uses
*/

// output will be true if one of the input return true

let ar1 = ["nitan", "ram", "hari"];

let hasNitan = ar1.some((value, i) => {
  if (value === "nitan") {
    return true;
  } else {
    return false;
  }
});
console.log(unknown);
