let name = null ?? undefined ?? null ?? undefined ?? null;
console.log(name);
//falsy values are (null, undefined)
// if value is falsy value see next one
// if all are false the output  will be last value
